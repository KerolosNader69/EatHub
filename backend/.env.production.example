# Production Environment Variables
# Copy this file and configure for your production environment

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration (MongoDB Atlas)
# Get this from MongoDB Atlas: Database > Connect > Connect your application
# Format: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/eathub?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/eathub?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_production_jwt_secret_key_here_use_crypto_to_generate
JWT_EXPIRE=24h

# CORS Configuration
# Set this to your frontend URL (e.g., https://your-app.vercel.app)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Logging Configuration
LOG_LEVEL=info
